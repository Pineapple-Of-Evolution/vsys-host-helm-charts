{{- if .Values.btc_lightning.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: lightning-rtl
  namespace: {{ .Release.Namespace | quote }}
spec:
  selector:
    app: lightning-rtl
  ports:
  - name: web
    port: 3000
    targetPort: web
  type: LoadBalancer

{{ end }}